import { ContratistasPipe } from './contratistas.pipe';

describe('ContratistasPipe', () => {
  it('create an instance', () => {
    const pipe = new ContratistasPipe();
    expect(pipe).toBeTruthy();
  });
});
